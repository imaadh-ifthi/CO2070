# Lecture: 2
**Reference material:** 
---

* registers are within the chip, register operands assume that data is in the register not memory.

* Data is not always 32 bit so memory is byte(8 bit) addressed, ergo words are memory aligned and addresses are a multiple of 4.

* ARM is little Endian

* '#' indicates a raw number in this instruction set

1. Consider the C code: 
    g = h + A[8];

    In ARM:
        LDR r5. [r3, #32]; reg 5 gets A[8], r3 is base reg and 32 is the offset
        ADD r1, r2, r5; g = h + A[8]
2. To store values in the new address:
    A[12] = h + A[8];

    In ARM:
        placeholder

* Registers are faster to access than memory and compiler must use regsiters for var as much as possible.

* Immediate Operands: cosntant data is sepcified in the instruction; this avoids loading

* Unsigned Bin Int range: 0 to $ [2^{n-1}] $

* 2's complement unsigned Bin Int range: $ [2^{n-1}] $ to $ [-2^{n-1}] $

* 2's complement signed Bin Int range: placeholder

* Signed negation: placeholder; use LaTex

* Signed extension: 
    * repping a number using more bits, 
    * for ARM we used LDRSB, LDRSH: extend loaded byte/halfword
    * replicate sign bit to the left

* ARM instructions are also 32 bits formatted within Instruction Fields.

* ARM Data Transfer Instruction (LDR)
    * box: cond, f, Opcode, Rn, Rd, Offset12 with vals 4,2,6,4,4,12 respectivelu.

* When designing ISA's you keep space for future improvements, so forwards compatible exist

* Shift Op format different

* In Conditional Operations we,
    * cmp the vals
    * Branch
That is we branch to a labelled instruction if a condition is true, otherwise continue sequentially

* Whatever is between entry and exit pts is called basic blocks cuz you can't break/come out of that.

* Signed comparison: BGE, BLT, BGT, BLE
  Unsigned comparison: BHS, BLO, BHI, BLS

* Status register/ plaid register will have dif bits to store result of substraction. The cmp and branch instructions will look at these; refer later

* In assembly the physical components matter, in langs like C this si abstracted away to the compiler.

* Stack pointer is preserved on call since we need to store intermediate values while executing functions; stack ops are known as popping and pushing.

* Link Register is also similarly preserved so the function knows where to return to (main program)

* Proceedure Call Instructions
    * Branch and Link
    * Proceedure Return







